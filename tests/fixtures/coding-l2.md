# Source-of-truth and repository baseline
Task TASK-ONE delivers the user-visible result.
Module MOD-ONE implements VIEW-HOME and ACT-SUBMIT with field/data and state contracts.
API command enforces permission scope and audit and emits an event. Tests bind
AC-ONE and write evidence. Forbidden: do not invent behavior. Migration,
rollback, and operations/monitoring are documented.
