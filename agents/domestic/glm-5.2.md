# GLM 5.2 Adapter

Use concise Chinese instructions, preserve stable IDs verbatim, and request one
module/flow slice at a time for large projects. Require structured YAML/Markdown
outputs and run deterministic validators after generation. Never let context
compression remove roles, permissions, state transitions, exceptions, or P0 ACs.
Use deterministic output schemas and explicit completion gates. Large ToB/ToG
work must use progressive Product Truth fragments and section-sliced complete
AI Coding PRDs; compile and globally validate only after local checkpoints pass.
