# DeepSeek Adapter

Separate analysis from the delivery artifact. Pin business invariants and
forbidden invention before coding handoff. Use small vertical slices and run
PRD, IA, Coding Agent, and Product Truth validators after every accepted slice.
Use explicit source precedence, stable IDs, and one output file per module.
For complete AI Coding PRDs, draft by required contract section, checkpoint,
then run the structural validator. Do not compress missing sections into an
overview and do not generate a large Product Truth monolith in one response.
