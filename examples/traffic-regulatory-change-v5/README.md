# Traffic Regulatory Change Package

Demonstrates a regulated-source update as a scoped Change Package rather than a
full PRD regeneration. The fixture is proposed/not-run and does not claim that
the referenced product has implemented the change.

```powershell
py -3 scripts/validate_change_package.py examples/traffic-regulatory-change-v5/change-package.yaml
```
